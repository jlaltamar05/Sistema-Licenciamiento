// Configuración compartida por todos los módulos del frontend.
// Si algún día cambias el puerto o el dominio de tu API, solo se
// modifica aquí — ningún otro archivo necesita tocarse.

// Vacío = "misma dirección donde está sirviéndose esta página".
// Funciona igual en tu computadora (localhost:3000) y en producción
// (Render), sin tener que cambiar nada entre los dos.
const API_BASE_URL = '';
